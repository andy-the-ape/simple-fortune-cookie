apiVersion: v1
kind: Service
metadata:
  labels:
    app: cookie-app
  name: backend
spec:
  ports:
    - port: 9000
      protocol: TCP
      targetPort: 9000
  selector:
    app: backend
  type: ClusterIP
